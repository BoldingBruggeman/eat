# This script is intended to be source'd, not executed

set -e

REPO_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Build eat
WORK_DIR=`mktemp -d`
cd ${WORK_DIR}
cmake ${REPO_DIR}
make -j4 install
cd -
rm -rf ${WORK_DIR}
